#######################################
# Changes into a random directory at
# the given path (non-recursive).
#
# Args:
#     The base path
#######################################
cd_random_subdir() {
    cd $(ls -d $1/* | shuf | head -n 1)
}
